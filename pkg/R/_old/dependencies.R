
Sys.setenv(NOAWT = TRUE)

suppressPackageStartupMessages({
   
   library(inspect)
   library(stats)
   library(lsa)
   library(tm)
   library(readbitmap)
   library(oce)
   library(network)
   library(lattice)
   library(fields)
   library(sna)
   library(Matrix)
   library(plotrix)
   library(digest)
   library(tools)
   library(lattice)
   library(scales)
   library(cluster)

   library(XML)
   library(pracma)
   
   #library(scatterplot3d)
   #library(rgl)
   
})