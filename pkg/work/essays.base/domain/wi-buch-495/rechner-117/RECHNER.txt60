Nur-Lesespeicher beziehungsweise Fest(wert)speicher, so genannte ROM (Abkürzung für engl.: read only memory), bei denen man einige Varianten (FROM, PROM, EPROM, EEPROM, Flash) danach unterscheidet, ob der beliebig oft lesbare Inhalt irreversibel oder reversibel eingeschrieben wird und auf welche Weise dies geschieht. Die ROM-Varianten werden in diesem Abschnitt noch näher erklärt. 